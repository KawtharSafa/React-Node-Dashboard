# run backend

npm i
npm run dev / nodemon

# if no more data in /data/users.json -> generate data from seed.js:

1. stop server CTRL + C
2. make sure of terminal path : cd server
3. run seed.js: npm run seed
4. re-run server: nodemon
